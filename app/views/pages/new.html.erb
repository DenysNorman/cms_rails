<%=  form_for @page do |f|%>
    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.fields_for :blocks do |block| %>
      <%=  render 'block_blocks', f: block %>
    <% end %>
    <%= link_to_add_fields 'Add Block', f, :blocks %>
    <%= f.submit %>
<% end %>